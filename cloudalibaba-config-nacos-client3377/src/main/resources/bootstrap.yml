# nacos配置
server:
  port: 3377
spring:
  #  profiles:
  #    active: dev
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: wangyaochong:1111 #Nacos服务注册中心地址
      config:
        server-addr: wangyaochong:1111 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
#        group: DEV_GROUP
#        namespace: 8047633b-c4d1-4c6e-ba0a-f36be1735dd5


# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
# nacos-config-client-dev.yaml

# nacos-config-client-test.yaml   ----> config.info